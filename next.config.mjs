/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "fiverrnew.cybersoft.edu.vn",
           "sc04.alicdn.com"
        ],
    },
};

export default nextConfig;
